---
import BaseLayout from "./BaseLayout.astro"
import { ArticleLayout } from "@/components/ArticleLayout";
const { frontmatter } = Astro.props;
const { title, date } = frontmatter
---

<BaseLayout title="yet-to-figure-out">
  <ArticleLayout meta={{
    title,
    date
  }}>
    <slot/>
  </ArticleLayout>
</BaseLayout>
